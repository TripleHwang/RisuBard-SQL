# RisuBard Core

This directory contains RisuBard's UI-independent core contracts, deterministic context compiler, and grounded `MemoryDelta` reducer.

PocketRisu code must not be copied or mechanically translated into this package. See `docs/architecture/code-boundaries.md`.
