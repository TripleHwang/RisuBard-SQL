# RisuVault 0.3.2.7

- `v0.3.2.6` 이전에 SQL 이전을 완료한 설치에서도 보존된 레거시 데이터베이스를 사용해 축소된 캐릭터 본문을 복구합니다. 복구 실패 시 실제 SQL 또는 HTTP 원인을 표시합니다.
- RisuRealm 탐색 요청이 일시적인 네트워크 오류 또는 `502`~`504` 응답으로 실패하면 한 번 자동 재시도하고, 최종 실패 원인을 브라우저 콘솔에 기록합니다.
